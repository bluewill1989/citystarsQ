<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 公共信息，也可以用dubbo.properties配置 -->
    <dubbo:application name="citystarsQ" />
    <dubbo:registry address="${zookeeper_url}" file="dubbo/dubbo.cache.${SERVER_NAME}" />
    <dubbo:protocol name="dubbo" port="-1" threads="500" />
    <dubbo:provider filter="traceid" />
    <dubbo:consumer filter="traceid" />
    <dubbo:monitor protocol="registry" />
    
    <dubbo:service interface="com.citystars.common.api.service.dubbo.DemoService"
                   ref="demoServiceImpl" version="1.0" timeout="5000" cluster="failover"
    ></dubbo:service>



</beans>